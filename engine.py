class AAMEngine:

    def calculate(self, tsh, t4, atpo):

        return {
            "status": "Demo mode",
            "message":
            "Core analytical algorithms are not included in the public repository."
        }
